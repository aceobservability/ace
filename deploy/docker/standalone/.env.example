# Copy to .env and set JWT_SECRET before starting:
#   cp .env.example .env
#
# Generate a secret:
#   openssl rand -base64 48

JWT_SECRET=

# Optional overrides
# ACE_PORT=8080
# ACE_STANDALONE_IMAGE=ghcr.io/aceobservability/ace-standalone:latest
# POSTGRES_USER=ace
# POSTGRES_PASSWORD=ace
# POSTGRES_DB=ace